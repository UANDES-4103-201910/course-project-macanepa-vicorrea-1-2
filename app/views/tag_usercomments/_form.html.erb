<%= form_with(model: tag_usercomment, local: true) do |form| %>
  <% if tag_usercomment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tag_usercomment.errors.count, "error") %> prohibited this tag_usercomment from being saved:</h2>

      <ul>
      <% tag_usercomment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :comment_id %>
    <%= form.text_field :comment_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
