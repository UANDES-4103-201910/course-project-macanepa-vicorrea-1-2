<%= form_with(model: follow_post, local: true) do |form| %>
  <% if follow_post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(follow_post.errors.count, "error") %> prohibited this follow_post from being saved:</h2>

      <ul>
      <% follow_post.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :post_id %>
    <%= form.text_field :post_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
