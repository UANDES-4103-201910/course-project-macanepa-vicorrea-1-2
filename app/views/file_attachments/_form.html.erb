<%= form_with(model: file_attachment, local: true) do |form| %>
  <% if file_attachment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(file_attachment.errors.count, "error") %> prohibited this file_attachment from being saved:</h2>

      <ul>
      <% file_attachment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :post_id %>
    <%= form.text_field :post_id %>
  </div>

  <div class="field">
    <%= form.label :file %>
    <%= form.text_field :file %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
