<%= form_with(model: shared_post, local: true) do |form| %>
  <% if shared_post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(shared_post.errors.count, "error") %> prohibited this shared_post from being saved:</h2>

      <ul>
      <% shared_post.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :post_id %>
    <%= form.text_field :post_id %>
  </div>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_area :content %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
